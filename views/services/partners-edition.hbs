<div class="container-sm">
  <h2>Would you like to change any information about the service?</h2>
  <div class="d-flex justify-content-center">
    <form
      action="/puppy-care/{{service._id}}/partners-edition"
      method="post"
      enctype="multipart/form-data"
    >
      <div class="form-row">
        <label for="name">Name</label>
        <input
          type="text"
          name="name"
          class="form-control"
          value="{{service.name}}"
        />
      </div>
      <div class="form-row">
        <label for="serviceType">What kind of business do you have?</label>
        <select
          name="serviceType"
          class="form-control"
          value="{{service.serviceType}}"
        >
          <option>Select...</option>
          <option value="PuppySitter">Puppysitter</option>
          <option value="Hairdresser">Hairdresser</option>
          <option value="PuppyTrainer">Puppy trainer</option>
          <option value="Veterinary">Veterinary</option>
        </select>
      </div>
      <div class="form-row">
        <label for="address">Address</label>
        <input
          type="text"
          name="address"
          class="form-control"
          value="{{service.address}}"
        />
      </div>
      <div class="form-row">
        <label for="contact">Contact</label>
        <input
          type="text"
          name="contact"
          class="form-control"
          value="{{service.contact}}"
        />
      </div>
      <div class="form-row">
        <label for="image">Business photo</label>
        <input
          type="file"
          name="image"
          class="form-control"
          value="{{service.image}}"
        />
      </div>
      <br />
      <br />

      <button type="submit" class="btn btn-success">Update the service</button>

    </form>
  </div>
  <br />
  <br />
  <div class="d-flex justify-content-center">

    <form
      action="/puppy-care/{{service._id}}/partners-edition/delete"
      method="post"
    >
      <button type="submit" class="btn btn-success">I don't want to offer this
        service anymore.</button>
    </form>

  </div>
  <a href="/puppy-care/partners-form">Back to partners form</a>
</div>